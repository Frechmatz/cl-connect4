
(in-package :ccfi)

(defun encode-board (width height getTokenFn)
  "Create the ccfi representation of a board
getTokenFn (x y)"

  (declare (ignore width))
  (declare (ignore height))
  (declare (ignore getTokenFn))

  )


